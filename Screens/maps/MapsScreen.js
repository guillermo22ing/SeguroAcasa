import React from 'react';
import { View, Text } from 'react-native';
import mapStyles from '../../Styles/mapsStyles/mapsStyles';

//aqui agrega la logica para el mapa, o depende

const Maps = () => {
  return (
    <View style= {mapStyles.container}>
      <Text>mapa</Text>
    </View>
  );
};

export default Maps;

